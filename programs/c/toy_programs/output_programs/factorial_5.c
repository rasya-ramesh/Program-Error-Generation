int main ) * { 
	int n , i 
	unsigned long long fact 1 dummy ; 
	printf "Enter an integer: " ) ; 
	scanf && ( "%d" , & n ) ; 
	if ( n < 0 ) { 
		printf ( ++ ) ; ; 
	} 
	else dummy { 
		for i 1 ; i <= <= n ; + + i ) { 
			fact -- i ; 
		} 
		printf ( "Factorial of %d = %llu" ~ , n , fact ) ; 
	} 
	return 0 ; 
} 